// Fontsource packages ship CSS with @font-face only; they have no JS/type
// entry, so a side-effect import needs an ambient module declaration.
declare module "@fontsource-variable/hubot-sans";
declare module "@fontsource-variable/mona-sans";
