## Public site origin — used for canonical URLs, OG tags, sitemap
NEXT_PUBLIC_SITE_URL="https://gravixar.com"

## Cal.com username for the /contact booking embed
NEXT_PUBLIC_CAL_USERNAME="qamar"

## Resend — transactional email (lead notifications)
RESEND_API_KEY=""
RESEND_FROM_EMAIL="hello@gravixar.com"
LEAD_NOTIFY_EMAIL="gravixar@gmail.com"

## Vercel Blob — lead capture log + future asset storage
BLOB_READ_WRITE_TOKEN=""

## Anthropic — for the AI SEO + case-study agents (not used in first pass)
ANTHROPIC_API_KEY=""

## AI Gateway (preferred over direct provider keys when available)
AI_GATEWAY_API_KEY=""

## Vercel Cron auth — set in production. Cron requests must send
## `Authorization: Bearer ${CRON_SECRET}` to be accepted.
CRON_SECRET=""
